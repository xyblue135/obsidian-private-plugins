# PR 工作流程（本仓库）

本仓库所有改动统一走 Pull Request，不直接推送 `main`。

## 流程

1. 基于 `main` 最新提交开临时分支 `pr/<topic>`。
2. 改动落到该分支（文件增删改）。
3. 提 PR 到 `main`。
4. 合并后删除 `pr/<topic>` 分支。

## 约定

- `pr/*` 是临时分支，合并即删；`main` 是唯一长存分支。
- 本地 `.git` 在本环境不稳定，所有 git 操作走 GitHub API（`gh api`）。
- 插件 `data.json` 中的 API 均为内网地址，外部不可达，可安全公开。

> 本文件本身是一次「推送测试」PR 的产物，用于验证 PR 流程可用。
